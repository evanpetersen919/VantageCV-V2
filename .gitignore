# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
.venv/
venv/
.mypy_cache/
.pytest_cache/
.ipynb_checkpoints/
htmlcov/
.coverage
.coverage.*
coverage.xml
dist/
build/

# Sphinx
docs/_build/

# Poetry
poetry.lock.tmp

# UE5
unreal_plugin/**/Binaries/
unreal_plugin/**/Intermediate/
unreal_plugin/**/DerivedDataCache/
unreal_plugin/**/Saved/
ue5_project/Binaries/
ue5_project/Intermediate/
ue5_project/Saved/
ue5_project/DerivedDataCache/
*.uasset
*.umap

# Datasets / large outputs
datasets/
*.pcd
*.h5

# Editor
.vscode/
.idea/

# OS
.DS_Store
Thumbs.db

# Local-only planning/spec docs (kept on disk, not published to the
# public repo -- extensively cross-referenced by section name throughout
# KNOWN_GAPS_AND_ISSUES.md, docstrings, and test comments, so those
# references will point to files a repo visitor can't see; that's a
# known, accepted tradeoff, not an oversight -- see the commit that
# added this.
MASTER_PROMPT_PROCEDURAL_AV_DATASET_GENERATOR.md
HOW_TO_USE_MASTER_PROMPT.md
CLAUDE_SKILLS_AND_PROMPTS.md
QOL_RESEARCH_CHECKLIST.md
